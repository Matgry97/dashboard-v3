# TODO

## Strava Integration

- [ ] **Build Strava server integration** — `server/integrations/strava/` with last-workout endpoint
- [ ] **Build last-workout widget** — replace Garmin widget with Strava data

## Pi Deployment

- [ ] **systemd service** — set up Express as a systemd service so it starts on boot
- [ ] **Repeat Strava setup on the Pi** — add `STRAVA_CLIENT_ID`, `STRAVA_CLIENT_SECRET`, `STRAVA_REFRESH_TOKEN` to `.env`
